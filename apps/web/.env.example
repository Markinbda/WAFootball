# Copy to .env.local (apps/web/.env.local) and fill in real values.
# Never commit .env.local — only .env.example.

VITE_SITE_NAME="Warwick FA"

# Supabase project (https://app.supabase.com → Project Settings → API)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
